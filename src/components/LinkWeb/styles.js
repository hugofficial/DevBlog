import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    padding: 10,
    backgroundColor: "#232630",
    marginTop: 75,
    flexDirection: "row",
    alignItems: "center",
  },

  name: {
    color: "#fff",
    marginLeft: 8,
    fontSize: 18,
    fontWeight: "bold",
  },
});
